<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificar Conta - Central RPG</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Special+Elite&family=Oswald:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Oswald', sans-serif;
            background: linear-gradient(-45deg, #232526, #414345, #1a1a1a, #000000);
            background-size: 400% 400%;
            animation: gradientBG 25s ease infinite;
        }
        @keyframes gradientBG { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
        .font-typewriter { font-family: 'Special Elite', cursive; }
        .glass-card {
            background: rgba(10, 10, 10, 0.6);
            -webkit-backdrop-filter: blur(10px);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
    </style>
</head>
<body class="text-gray-200 flex items-center justify-center min-h-screen p-4">

<div class="glass-card w-full max-w-md p-8 rounded-lg shadow-2xl shadow-black/50">
    <header class="text-center mb-8">
        <h1 class="font-typewriter text-4xl font-bold text-yellow-400" style="text-shadow: 0 0 10px #f6e05e;">Verificar Conta</h1>
        <p class="text-gray-400">Insira o código que enviámos para o seu e-mail.</p>
    </header>

    <?php if (isset($erro)): ?>
        <!-- Bloco de Erro -->
        <div class="bg-red-900/50 border border-red-500 text-red-300 p-3 rounded-lg mb-6 text-center">
            <p><?php echo htmlspecialchars($erro); ?></p>
        </div>
    <?php endif; ?>

    <!-- Formulário de Verificação -->
    <form action="/verificar" method="POST">
        <div class="space-y-6">
            <div>
                <label for="codigo" class="block text-sm font-bold text-gray-400 mb-2">Código de 6 Dígitos</label>
                <input
                    type="text"
                    id="codigo"
                    name="codigo"
                    class="w-full bg-gray-900/50 border border-gray-600 rounded p-3 text-center text-2xl tracking-[10px] focus:outline-none focus:ring-2 focus:ring-yellow-500 transition-all"
                    maxlength="6"
                    required
                    autofocus
                >
            </div>
        </div>

        <div class="mt-8">
            <button type="submit" class="w-full bg-yellow-600 hover:bg-yellow-700 text-black font-bold py-3 px-4 rounded-lg transition-colors duration-300 shadow-lg shadow-yellow-900/50">
                Verificar Código
            </button>
        </div>
    </form>

</div>

</body>
</html>