<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sala: <?php echo htmlspecialchars($sala->getNomeSala()); ?> - Central RPG</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Special+Elite&family=Oswald:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Oswald', sans-serif; background: linear-gradient(-45deg, #232526, #414345, #1a1a1a, #000000); background-size: 400% 400%; animation: gradientBG 25s ease infinite; }
        @keyframes gradientBG { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
        .font-typewriter { font-family: 'Special Elite', cursive; }
        .glass-card { background: rgba(10, 10, 10, 0.6); -webkit-backdrop-filter: blur(10px); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.1); }
        .log-mestre { border-left: 3px solid #e53e3e; }
        .log-jogador { border-left: 3px solid #4299e1; }
        .log-sistema { border-left: 3px solid #f6e05e; }
    </style>
</head>
<body class="text-gray-200">

<div class="flex flex-col h-screen">
    <!-- Cabeçalho -->
    <header class="glass-card flex-shrink-0 p-4 flex justify-between items-center">
        <h1 class="font-typewriter text-2xl md:text-3xl font-bold text-green-400"><?php echo htmlspecialchars($sala->getNomeSala()); ?></h1>
        <a href="/dashboard" class="bg-blue-700 hover:bg-blue-800 text-white font-bold py-2 px-4 rounded-lg transition-colors">&larr; Painel</a>
    </header>

    <!-- Corpo Principal -->
    <div class="flex-grow flex flex-col md:flex-row gap-4 p-4 overflow-hidden">

        <!-- Coluna Principal: Log e Formulário -->
        <main class="flex-grow flex flex-col glass-card rounded-lg p-4 overflow-hidden">
            <!-- Log do Jogo -->
            <div id="log-container" class="flex-grow overflow-y-auto flex flex-col-reverse p-2 bg-black/20 rounded-lg">
                <div class="space-y-4">
                    <?php if (empty($logs)): ?>
                        <p class="font-typewriter text-gray-500 text-center p-4">A aventura ainda não começou. O silêncio é pesado...</p>
                    <?php endif; ?>
                    <?php foreach (array_reverse($logs) as $log): ?>
                        <div class="p-3 rounded-r-lg font-typewriter text-sm log-<?php echo $log->tipoLog; ?>">
                            <p class="font-sans font-bold text-xs uppercase text-<?php echo $log->tipoLog === 'mestre' ? 'red' : ($log->tipoLog === 'jogador' ? 'blue' : 'yellow'); ?>-400"><?php echo htmlspecialchars($log->autorNome); ?></p>
                            <p class="text-gray-300 whitespace-pre-wrap"><?php echo htmlspecialchars($log->mensagem); ?></p>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
            <!-- Formulário de Publicação -->
            <form action="/sala/<?php echo $sala->id; ?>/publicar" method="POST" class="flex-shrink-0 mt-4">
                <textarea name="mensagem" class="w-full bg-gray-900/50 border border-gray-600 rounded-t-lg p-3 focus:outline-none focus:ring-2 focus:ring-green-500" rows="3" placeholder="O que você faz, <?php echo htmlspecialchars($nomeAutor); ?>?"></textarea>
                <button type="submit" class="w-full bg-green-700 hover:bg-green-800 text-white font-bold py-2 rounded-b-lg transition-colors">Publicar Ação</button>
            </form>
        </main>

        <!-- Barra Lateral: Participantes -->
        <aside class="flex-shrink-0 w-full md:w-64 lg:w-72 glass-card rounded-lg p-4 overflow-y-auto">
            <h2 class="text-xl font-bold text-yellow-400 border-b border-gray-700 pb-2 mb-4">Participantes</h2>
            <div class="space-y-3">
                <?php foreach ($participantes as $participante): ?>
                    <div class="bg-black/20 p-3 rounded-lg">
                        <p class="font-bold text-white"><?php echo htmlspecialchars($participante['nome_usuario']); ?></p>
                        <?php if ($sala->idMestre === $participante['id_usuario']): ?>
                            <p class="text-xs text-red-400">Mestre da Sala</p>
                        <?php elseif (!empty($participante['nome_personagem'])): ?>
                            <p class="text-sm text-gray-300">a jogar como <strong class="text-green-300"><?php echo htmlspecialchars($participante['nome_personagem']); ?></strong></p>
                        <?php else: ?>
                            <p class="text-sm text-gray-500">(Ainda não escolheu um personagem)</p>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
            </div>
        </aside>

    </div>
</div>
<script>
    // Scroll automático para o fundo do log
    const logContainer = document.getElementById('log-container');
    logContainer.scrollTop = logContainer.scrollHeight;
</script>
</body>
</html>