<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Nova Sala - Central RPG</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Special+Elite&family=Oswald:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Oswald', sans-serif; background: linear-gradient(-45deg, #232526, #414345, #1a1a1a, #000000); background-size: 400% 400%; animation: gradientBG 25s ease infinite; }
        @keyframes gradientBG { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
        .font-typewriter { font-family: 'Special Elite', cursive; }
        .glass-card { background: rgba(10, 10, 10, 0.6); -webkit-backdrop-filter: blur(10px); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.1); }
    </style>
</head>
<body class="text-gray-200 flex items-center justify-center min-h-screen p-4">

<div class="glass-card w-full max-w-2xl p-8 rounded-lg shadow-2xl shadow-black/50">
    <header class="text-center mb-8">
        <h1 class="font-typewriter text-4xl font-bold text-green-400" style="text-shadow: 0 0 10px #2f855a;">Criar Nova Sala</h1>
        <p class="text-gray-400">Defina os detalhes da sua próxima grande aventura.</p>
    </header>

    <?php if (isset($erro)): ?>
        <div class="bg-red-900/50 border border-red-500 text-red-300 p-3 rounded-lg mb-6 text-center">
            <p><?php echo htmlspecialchars($erro); ?></p>
        </div>
    <?php endif; ?>

    <form action="/salas/criar" method="POST">
        <div class="space-y-6">
            <div>
                <label for="nome_sala" class="block text-sm font-bold text-gray-400 mb-2">Nome da Sala</label>
                <input type="text" id="nome_sala" name="nome_sala" class="w-full bg-gray-900/50 border border-gray-600 rounded p-3 focus:outline-none focus:ring-2 focus:ring-green-500 transition-all" required>
            </div>
            <div>
                <label for="id_sistema" class="block text-sm font-bold text-gray-400 mb-2">Sistema / Categoria de Jogo</label>
                <select id="id_sistema" name="id_sistema" class="w-full bg-gray-900/50 border border-gray-600 rounded p-3 focus:outline-none focus:ring-2 focus:ring-green-500 transition-all" required>
                    <option value="" disabled selected>-- Escolha um sistema --</option>
                    <?php foreach ($sistemas as $sistema): ?>
                        <option value="<?php echo $sistema->id; ?>">
                            <?php echo htmlspecialchars($sistema->nomeSistema); ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>

        <div class="mt-8 flex gap-4">
            <a href="/dashboard" class="w-1/2 text-center bg-gray-700 hover:bg-gray-600 text-white font-bold py-3 px-4 rounded-lg transition-colors">Cancelar</a>
            <button type="submit" class="w-1/2 bg-green-700 hover:bg-green-800 text-white font-bold py-3 px-4 rounded-lg transition-colors duration-300 shadow-lg shadow-green-900/50">
                Criar Sala
            </button>
        </div>
    </form>
</div>

</body>
</html>